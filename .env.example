# AWS Bedrock Configuration
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Bedrock Model IDs (keep these as-is)
BEDROCK_MODEL_CLAUDE=anthropic.claude-3-5-sonnet-20241022-v2:0
BEDROCK_MODEL_NOVA=amazon.nova-pro-v1:0

# Temporal Configuration (keep these as-is)
TEMPORAL_NAMESPACE=default
TEMPORAL_TASK_QUEUE=loan-underwriter-queue

# MCP Server Configuration (keep these as-is)
MCP_SERVER_HOST=localhost
MCP_SERVER_PORT=3100

# API Configuration (keep these as-is)
BACKEND_PORT=8000
FRONTEND_PORT=3000
